import TaskbarProps from "../taskBarProps";

const Intermediate = () => {
  return (
    <div>
      <span className="font-bold text-[22px]">Intermediate Content</span>
      <TaskbarProps stt={1} title="Intermediate Content" onclick={() => {}} />
    </div>
  );
};

export default Intermediate;
